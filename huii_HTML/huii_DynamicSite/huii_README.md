# huii_Canvas Hero Layout - "Motion"

A creative technical experiment where a complete website hero section—including navigation, typography, buttons, and UI mockups—is rendered entirely using the HTML5 Canvas API.

## 🚀 Features
* **Canvas Rendering:** The entire landing page UI is drawn via JavaScript using `getContext('2d')`.
* **Dynamic Background:** A smooth, 4-color animated CSS gradient background that shifts over time.
* **UI Elements:** Features a mock navigation bar, a multi-line bold headline, call-to-action buttons, and abstract UI components.
* **Vector Graphics:** Custom shapes, arcs, and paths used to create modern geometric illustrations within the canvas.

## 🛠️ Built With
* **HTML5 Canvas:** For the primary UI rendering.
* **JavaScript:** Used to define fills, strokes, gradients, and typography positions.
* **CSS3:** For the animated background and responsive canvas centering.

## 📁 File Structure
* `index.html` - Contains the HTML structure, CSS animations, and the JavaScript canvas logic.

## ⚙️ Setup
Open `index.html` in any web browser. The UI will render instantly on the canvas.
