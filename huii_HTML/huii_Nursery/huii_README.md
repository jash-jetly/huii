# huii_Petal & Root — Botanical Nursery

A sophisticated e-commerce landing page for a premium plant nursery. The project features a refined aesthetic with a focus on high-end typography and smooth user interactions.

## 🚀 Features
* **Dynamic Gallery Filtering:** Interactive category buttons (Perennials, Annuals, Tropical, etc.) that filter plant cards with smooth fade animations.
* **Responsive Masonry Layout:** A multi-column gallery that automatically adjusts from a 4-column desktop view down to a single-column mobile view.
* **Interactive UI Elements:** * "Save" heart buttons with persistent state toggling.
    * Staggered "fade-up" entry animations for all product cards.
    * Sticky header with a modern frosted-glass (blur) effect.
* **Visual Polish:** Uses a unique "spinning" logo icon and stylized italics to emphasize the "Artisan" botanical brand.

## 🛠️ Built With
* **HTML5:** Semantic structure including `<article>` cards and SVG icons.
* **CSS3:** Advanced techniques like `columns` for masonry layouts, CSS variables, and custom keyframe animations.
* **JavaScript (Vanilla):** Light logic for category filtering and UI interaction states.
* **Google Fonts:** 'Cormorant Garamond' for an elegant serif feel and 'Jost' for modern readability.

## 📁 File Structure
* `index.html` - The core structure, product data, and interaction scripts.
* `style.css` - Custom styling, responsive breakpoints, and animations.

## ⚙️ Setup
Open `index.html` in any modern web browser to view the nursery catalog.
