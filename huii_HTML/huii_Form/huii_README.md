# huii_Local Storage Registration Form

A functional, responsive registration form that uses browser Local Storage to save and persist user data without a backend database.

## 🚀 Features
* **Persistent Data:** Saves user input (Name, Email, Gender, Mobile) to `localStorage` so data remains even after refreshing the page.
* **Instant Feedback:** Displays the saved registration details dynamically on the screen upon submission.
* **Data Management:** Includes a "Clear Saved Data" feature to reset the storage and UI.
* **Modern Design:** Features a purple-to-blue linear gradient background and a clean "card-style" form layout.
* **Fully Responsive:** Optimized for mobile devices with stackable radio buttons and adjusted padding for smaller screens.

## 🛠️ Built With
* **HTML5** - Form structure and semantic elements.
* **CSS3** - Custom transitions, flexbox centering, and gradient overlays.
* **JavaScript (Vanilla)** - Handles form submission, Local Storage API, and DOM manipulation.

## 📁 File Structure
* `index.html` - The form structure and JavaScript logic.
* `style.css` - Custom styling and responsive media queries.

## ⚙️ Setup
1. Open `index.html` in any web browser.
2. Fill out the form and click **Register**.
3. Refresh the page to see that your data stays saved!
